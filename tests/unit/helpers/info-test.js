import { info } from 'unitis/helpers/info';
import { module, test } from 'qunit';

module('Unit | Helper | info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = info([42]);
  assert.ok(result);
});
