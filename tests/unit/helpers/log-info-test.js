import { logInfo } from 'unitis/helpers/log-info';
import { module, test } from 'qunit';

module('Unit | Helper | log info');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = logInfo([42]);
  assert.ok(result);
});
